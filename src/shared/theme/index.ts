import { DefaultTheme, Theme } from "react-native-paper";

export const theme: Theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    accent: '#656565',
    primary: '#283593',
    background: '#D9D9D9'
  }
}
